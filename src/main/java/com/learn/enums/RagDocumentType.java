package com.learn.enums;

public enum RagDocumentType {
    /** Plain text entered directly by the admin. */
    TEXT,
    /** Content extracted from an uploaded file (PDF, DOCX, TXT, etc.). */
    FILE
}
